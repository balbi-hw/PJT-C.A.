# 24TH MAR

목표 사항들은 전부 달성했다.
  
과정에서 `scanner.close()` 의 사용법과 사용 의의를 배웠다.  
지금은 문제가 되지 않지만 추후 외부 데이터베이스 같은 곳에서 값을 받아올 때 닫지 않으면 문제가 될 수 있다고 한다.
  
또 메서드를 나름대로 잘 분리했다고 생각했다. 그런데 분리는 잘 했지만 이렇게 될거면
그냥 메인 메서드가 아니라 다른 `Service` 파일을 만들고 메인 메서드는 트리거 역할만 하는 것도 좋아보인다.  
`card` 데이터를 사용하는 메서드는 전부 `WordCard` 클래스에 몰아 넣고 `Service`에서 호출만 하는, 그리고
메인 메서드는 Service 메서드를 시작하는 트리거 역할만 하는게 더 깔끔해보인다.
  
동적 배열 ( ArrayList<>() ) 또한 배웠는데 능숙하게 사용할 수 있도록 연습해야겠다.
  
Scanner 클래스도 메서드에서 매번 새 객체를 만들어야 싶었는데 그럴 필요 없이
메인 또는 서비스 메서드에서 생성한 Scanner 객체를 인수로 넘겨주면 매번 만들 필요가 없었다.